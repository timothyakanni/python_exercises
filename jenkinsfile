pipeline {
   agent any

   stages {
      stage('Build') {
         steps {
            echo 'Building application stage from SCM'
         }
      }
      stage('Test') {
         steps {
            echo 'Testing application stage from SCM'
         }
      }
      stage('Deploy') {
         steps {
            echo 'Deploying application stage from SCM'
         }
      }
   }
}
